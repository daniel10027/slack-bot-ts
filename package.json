{
  "name": "slack-bot-ts",
  "version": "1.0.0",
  "private": true,
  "description": "Slack bot in TypeScript using Bolt (Socket Mode).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p .",
    "check": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@slack/bolt": "^3.18.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
